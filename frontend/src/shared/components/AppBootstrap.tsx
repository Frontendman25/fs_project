'use client'

/**
 * Session bootstrap is handled by AuthProvider (silent refresh).
 * Kept as a no-op mount point for future app-wide init hooks.
 */
export function AppBootstrap() {
  return null
}
